import { Header } from '../../User';
import { SideBar } from '../SideBar';

export const MainLayout = () => {
  return (
    <div>
      <h3>MainLayout</h3>
      <Header />
      <SideBar />
    </div>
  );
};
